# ReadBoard Go Icons

The 1024 x 1024 light and dark masters are the source of truth for the Go app icon. Run `script/generate_app_icons.sh` after replacing either master. The local build and launch flow selects the current macOS appearance; set `READBOARD_ICON_APPEARANCE=light` or `dark` to override it.
